
Possible rake commands:

rake test
 compiles and executes all tests located in the test/ folder

rake analyse["path_to_file.bc"]
 runs the analysis on the provided .bc file


Running the debug analysis on a file:

./run.sh path_to_file.c
 compiles the C file and runs the analysis with debug output

